openapi: 3.1.0
info:
  title: Test Service
  version: 0.0.0
tags:
  - name: date
paths:
  /date:
    get:
      operationId: GetDate
      description: Get the current date
      parameters: [ ]
      responses:
        '200':
          description: The request has succeeded.
          content:
            text/plain:
              schema:
                description: The requested date
                type: string
                format: date-time
      tags:
        - date
components: { }
