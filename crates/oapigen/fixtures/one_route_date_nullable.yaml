wopenapi: 3.1.0
info:
  title: Test Service
  version: 0.0.0
tags: [ ]
paths:
  /nullable:
    get:
      operationId: GetNullableDate
      description: Get the current date or nothing
      parameters: [ ]
      responses:
        "200":
          description: The request has succeeded.
          content:
            application/json:
              schema:
                type: [ string, "null" ]
                format: date-time
components: { }
